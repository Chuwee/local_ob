type: string
enum:
  - ALL
  - RESTRICTED
description: Specifies how an entity's cookie configuration will apply to its channels. ALL will apply to all channels. RESTRICTED will only apply to selected channels (a list must therefore be present).

type: object
properties:
  enabled:
    type: boolean
  mode:
    type: string
    enum:
      - DEFAULT
      - REDIRECT
  domains:
    type: array
    items:
      $ref: './domain-setting-domain.yml'
    maxItems: 5
required:
  - enabled
  - mode
  - domains
additionalProperties: false
additionalProperties: false
type: object
properties:
  type:
    type: string
    enum:
      - NONE
      - RESTRICTED
    description: Specifies if the promotion is assigned to a collective
  id:
    type: number
    description: Collective id
  restrictive_sale:
    type: boolean
    description: If activated only the clients with the validated collective will be able to finish the purchase.
  box_office_validation:
    type: boolean
    description: Resticts if is mandatory to validate the collective in box office

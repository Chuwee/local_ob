additionalProperties: false
type: object
properties:
  id:
    type: integer
    format: int64
  status:
    $ref: sale-request-status.yml
  request_date:
    type: string
    format: date-time
  channel:
    type: object
    properties:
      id:
        type: integer
        format: int64
      name:
        type: string
    required:
      - id
      - name
  product:
    type: object
    properties:
      id:
        type: integer
        format: int64
      name:
        type: string
    required:
      - id
      - name
  producer:
    type: object
    properties:
      id:
        type: integer
        format: int64
      name:
        type: string
    required:
      - id
      - name
required:
  - status
  - request_date
  - channel
  - product
  - producer
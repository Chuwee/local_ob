additionalProperties: false
type: object
properties:
  send_criteria:
    type: string
    enum:
      - ALWAYS
      - ONLY_IF_VALIDATED
      - NEVER
  scope_ids:
    type: array
    items:
      type: integer

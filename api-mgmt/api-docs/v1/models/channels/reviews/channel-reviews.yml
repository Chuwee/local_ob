additionalProperties: false
type: object
properties:
  enable:
    type: boolean
  send_criteria:
    type: string
    enum:
      - ALWAYS
      - ONLY_IF_VALIDATED
      - NEVER
  send_time_unit:
    type: string
    enum:
      - DAYS
      - HOURS
  send_time_value:
    type: integer
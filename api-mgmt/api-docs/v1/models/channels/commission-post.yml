additionalProperties: false
type: object
required:
  - from
  - values
properties:
  from:
    type: number
    example: 2
  values:
    $ref: './commission-values.yml'
  currency_code:
    type: string
    description: "Currency code. When not provided, channel operator default currency will be used."

additionalProperties: false
type: object
properties:
  promotion_max:
    $ref: ../promotions/promotion-max-limit.yml
    description: Max usages for the channel promotion
  purchase_min:
    $ref: ../promotions/promotion-limit.yml
    description: Min tickets in cart needed to apply the channel promotion
  amount_min:
    $ref: ./channel-promotion-amount.yml
    description: Min amount in cart needed to apply the channel promotion

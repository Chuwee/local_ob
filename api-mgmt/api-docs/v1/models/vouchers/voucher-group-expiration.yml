additionalProperties: false
type: object
properties:
  type:
    $ref: voucher-group-expiration-type.yml
  date:
    type: string
    format: date-time
    description: "Fixed datetime only for FIXED type"
  amount:
    type: integer
    description: "Amount of the time period to add only for RELATIVE type"
  time_period:
    $ref: voucher-group-expiration-time-period.yml
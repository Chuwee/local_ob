additionalProperties: false
type: object
required:
  - id
properties:
  id:
    type: integer
    format: int64
  status:
    type: string
    enum:
      - FREE
      - PROMOTER_LOCKED
      - KILL
  blocking_reason:
    type: integer
    format: int64
    description: By sending blocking reason, status PROMOTOR_LOCKED must be defined
  price_type:
    type: integer
    format: int64
  quota:
    type: integer
    format: int64
  visibility:
    $ref: ../../models/venue-template/visibility-type.yml
  accessibility:
    $ref: ../../models/venue-template/accessibility-type.yml
  gate:
    type: integer
    format: int64

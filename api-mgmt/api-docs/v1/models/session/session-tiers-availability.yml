type: array
items:
  type: object
  properties:
    price_type:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        capacity:
          type: integer
          format: int64
    quota:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        limit:
          type: integer
          format: int64
    tier:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        limit:
          type: integer
          format: int64
        active:
          type: boolean
    sold:
      type: integer
      format: int64
    refunded:
      type: integer
      format: int64

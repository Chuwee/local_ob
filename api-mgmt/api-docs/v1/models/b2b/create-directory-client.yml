additionalProperties: false
type: object
required:
  - clients
properties:
  entity_id:
    type: integer
    description: Only required for operator users
  clients:
    type: array
    items:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          description: Client id

additionalProperties: false
type: object
properties:
  season_ticket_id:
    description: Id of the season ticket
    type: integer
    format: int64
  session_id:
    description: Id of the session
    type: integer
    format: int64
  session_assigned:
    description: Result of the assignation, true if the session is assigned, false if the session is not assigned
    type: boolean
  reason:
    $ref: ../../models/season-ticket/season-ticket-session-assignation-reason.yml
    description: If the session is not assigned, describes the reason why this session is not assigned

additionalProperties: false
type: object
properties:
  sector_id:
    type: integer
    format: int64
  sector_name:
    type: string
  available_seats:
    type: integer
  rows:
    type: array
    items:
      $ref: available-row.yml
  not_numbered_zones:
    type: array
    items:
      $ref: available-not-numbered-zone.yml
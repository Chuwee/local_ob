  additionalProperties: false
  type: object
  properties:
    id:
      type: integer
    name:
      type: string
    active:
      type: boolean
    status:
      type: string
      enum:
        - ACTIVE
        - INACTIVE
        - PLANNED
    presale_period:
      $ref: ../session/session-presale-period.yml
    channels:
      type: array
      items:
        type: object
        properties:
          id:
            type: integer
            format: int64
          name:
            type: string
          selected:
            type: boolean
    customer_types:
      type: array
      items:
        type: object
        properties:
          id:
            type: integer
            format: int64
          name:
            type: string
          selected:
            type: boolean
    loyalty_program:
      type: object
      properties:
        enabled:
          type: boolean
        points:
          type: number
    member_tickets_limit_enabled:
      type: boolean
    member_tickets_limit:
      type: integer
      description: The maximum number of tickets assigned to the current member allowed to buy for this season ticket in this presale period
    general_tickets_limit:
      type: integer
      description: The maximum number of general public tickets allowed to buy to the current member for this season ticket in this presale period

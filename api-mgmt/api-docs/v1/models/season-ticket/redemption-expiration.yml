additionalProperties: false
type: object
properties:
  unit:
    type: string
    enum:
      - DAY
      - WEEK
      - MONTH
    description: Time unit for expiration calculation
  expires:
    type: integer
    minimum: 1
    description: Number of units after which redemption expires
description: Configuration for when ticket redemption expires

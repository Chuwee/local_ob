additionalProperties: false
type: object
properties:
  type:
    type: string
    enum:
      - EVENT
      - SESSION
  enabled:
    type: boolean
  price:
    type: object
    properties:
      type:
        type: string
        enum:
          - PRICE_WITH_RESTRICTIONS
          - ORIGINAL_PRICE
          - UNRESTRICTED
      restrictions:
        type: object
        properties:
          min:
            type: number
            format: double
          max:
            type: number
            format: double
  commission:
    type: object
    properties:
      percentage:
        type: number
        format: double
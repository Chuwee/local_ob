type: object
required:
  - from
  - to
  - values
properties:
  from:
    type: number
    format: double
  to:
    type: number
    format: double
  values:
    type: object
    required:
      - fixed
      - percentage
      - min
      - max
    properties:
      percentage:
        type: number
        format: double
      min:
        type: number
        format: double
      max:
        type: number
        format: double
      fixed:
        type: number
        format: double


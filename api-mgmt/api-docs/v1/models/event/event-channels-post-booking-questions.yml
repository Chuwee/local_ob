type: object
required:
  - enabled
  - questions
  - channels
properties:
  enabled:
    type: boolean
  questions:
    type: array
    items:
      $ref: 'post-booking-question.yml'
  channels:
    $ref: 'post-booking-questions-channels.yml'

additionalProperties: false
type: object
properties:
  settings:
    $ref: event-channel-settings.yml
  use_all_quotas:
    type: boolean
    description: If disabled, custom quotas can be defined
  quotas:
    type: array
    nullable: true
    description: Ids of event quotas available for channel. For events of type ``ACTIVITY`` or ``THEME PARK`` only 1 quota by each template is allowed
    items:
      type: integer

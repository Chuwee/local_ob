type: object
required:
  - brands
properties:
  brands:
    type: array
    description: List of card brands valid for the benefit
    items:
      type: string
    example: [ "VISA", "AMEX" ]
allOf:
  - $ref: './benefit-group-config.yml'

type: object
required:
  - type
properties:
  type:
    type: string
    enum:
      - INSTALLMENTS
      - PRESALE
    description: Type of benefit
  bin_groups:
    type: array
    description: List of BIN groups
    items:
      $ref: ./bin-group.yml 
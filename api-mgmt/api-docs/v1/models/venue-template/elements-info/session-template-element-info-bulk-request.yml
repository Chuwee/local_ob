additionalProperties: false
type: object
properties:
  element_info:
    $ref: session-template-info-update-dto.yml
  update_all_elements_info:
    type: boolean
    description: Update all template element info. It will create a template element info if there is a type without it.
  elements_type_related_id_map:
    $ref: template-info-type-with-ids-map-dto.yml
additionalProperties: false
type: object
properties:
  id:
    type: string
    description: 'ID of template info. ```{type}_{id}```. ```type``` is one of ```[view, nnz, priceType]``` and ```id```  is ID of ```type``` in data base. Example: ```priceType_123```'
  tags:
    type: array
    items:
      type: string
    nullable: true
  default_info:
    $ref: aggregated-info-response-dto.yml
additionalProperties: false
type: object
properties:
  id:
    type: integer
  max_capacity:
    $ref: ../common/limitless-value.yml
  price_types:
    description: Capacity of each price-type for venue-templates of type ``ACTIVITY`` or ``THEME_PARK``
    type: array
    nullable: true
    items:
      type: object
      properties:
        id:
          type: integer
        capacity:
          $ref: ../common/limitless-value.yml

additionalProperties: false
type: object
properties:
  name:
    type: string
  image:
    type: string
    nullable: true
    description: Binary value encoded on base64. Its dimensions must be 550x340px and its size must not exceed 150KB.
  space_id:
    type: integer
  venue_id:
    type: integer
    description: Only modifiable for venue-templates of type ``ACTIVITY`` or ``THEME_PARK``
  groups:
    $ref: venue-template-group.yml
    description: Only available for venue-templates of type ``ACTIVITY`` or ``THEME_PARK``

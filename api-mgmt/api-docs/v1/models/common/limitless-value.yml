additionalProperties: false
type: object
required:
  - type
properties:
  type:
    type: string
    enum:
      - UNLIMITED
      - FIXED
  value:
    type: integer
    description: Field with value only if type is ``FIXED``

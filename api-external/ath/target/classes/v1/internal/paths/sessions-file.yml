get:
  summary: Get list of previous uploaded files
  description: Get list of previous uploaded files
  tags:
    - File management
  parameters:
    - in: header
      name: Authorization
      description: access_token
      required: true
      schema:
        type: string
    - in: path
      name: sessionId
      description: Session id
      schema:
        type: integer
      required: true
  responses:
    '200':
      description: 'Get uploaded files information'
      content:
        application/json:
          schema:
            $ref: '../models/automatic-sales-file-info.yml'

additionalProperties: false
type: object
required:
  - limit
  - offset
properties:
  limit:
    type: integer
    description: Limit (default 50, max 1000)
  offset:
    type: integer
    description: Offset (default 0)

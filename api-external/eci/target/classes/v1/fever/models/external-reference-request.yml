additionalProperties: false
type: "object"
properties:
  session_id:
    type: "integer"
    description: "Session id"
  event_id:
    type: "integer"
    description: "Event id (required if session_id is informed)"
  entity_id:
    type: "integer"
    description: "Entity id"
  external_reference:
    type: "string"
    description: "External reference to update (null if it needs to be removed)"

additionalProperties: false
type: object
properties:
  type:
    type: string
    enum:
      - MONTHLY
      - WEEKLY
  calendarSelectType:
    type: string
    enum:
      - BY_HOUR
      - BY_LIST
  enabled:
    type: boolean
additionalProperties: false
type: object
properties:
  start:
    description: Only for EVENT and SESSION elements.
    type: string
    format: date-time
  end:
    description: Only for EVENT and SESSION elements.
    type: string
    format: date-time
  saleStart:
    description: Only for EVENT and SESSION elements.
    type: string
    format: date-time
  saleEnd:
    description: Only for EVENT and SESSION elements.
    type: string
    format: date-time
  startTimeUnit:
    type: string
    description: Only for PRODUCT elements. Delivery date information.
    enum:
      - MINUTES
      - HOURS
      - DAYS
      - WEEKS
      - MONTHS
  startTimeValue:
    type: integer
    description: Only for PRODUCT elements. Delivery date information.
  endTimeUnit:
    type: string
    description: Only for PRODUCT elements. Delivery date information.
    enum:
      - MINUTES
      - HOURS
      - DAYS
      - WEEKS
      - MONTHS
  endTimeValue:
    type: integer
    description: Only for PRODUCT elements. Delivery date information.
  startUnconfirmed:
    type: boolean
    description: Only for SESSION elements.
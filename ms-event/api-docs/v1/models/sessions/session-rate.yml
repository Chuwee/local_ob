type: object
properties:
  id:
    type: integer
    description: Rate ID
  name:
    type: string
    description: Rate name
  description:
    type: string
    description: Rate description
  restrictive:
    type: boolean
    description: Informs if a rate is restrictive
  default:
    type: boolean
    description: Informs if a rate is the default rate
  translations:
    type: object
    additionalProperties:
      type: string
  rateGroup:
    $ref: '../events/rate-group-data.yml'
    description: Rate group data
type: object
properties:
  renewalStartingDate:
    type: string
    format: date-time
  renewalEndDate:
    type: string
    format: date-time
  renewalEnabled:
    type: boolean
  renewalInProcess:
    type: boolean
  autoRenewal:
    type: boolean
  renewalType:
    type: string
    enum:
      - XML_SEPA
      - CSV_IMPORT
  renewalSubstatus:
    type: string
  bankAccountId:
    type: integer
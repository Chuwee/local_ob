type: object
properties:
  seasonTicketRenewalCandidatesList:
    type: array
    items:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        compatible:
          type: boolean
        reasons:
          type: array
          items:
            type: string
            enum:
              - SEASON_TICKET_ON_SALE
              - SEASON_TICKET_WITH_DIFFERENT_MEMBER_MANDATORY
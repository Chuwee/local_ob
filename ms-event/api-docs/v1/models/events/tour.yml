type: object
properties:
  id:
    type: integer
  name:
    type: string
  status:
    $ref: tour-status.yml
  entity:
    type: object
    properties:
      id:
        type: integer
      name:
        type: string
  entityAdminId:
    type: integer
  events:
    type: array
    items:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        archived:
          type: boolean
        capacity:
          type: integer
        startDate:
          type: string
          format: date-time
        status:
          $ref: event-status.yml

additionalProperties: false
type: object
required:
  - defaultValue
properties:
  defaultValue:
    type: string
    description: Valor por defecto
  translations:
    type: object
    additionalProperties:
      type: string
    example:
      "en-EN": "Cell Number"
    description: The customer's translation for every appearance of this key in the export.
additionalProperties: false
type: object
properties:
  name:
    type: string
  capacity:
    type: integer
  validDate:
    type: string
    format: date-time
  conditionTypes:
    type: array
    items:
      type: string
      enum:
      - DATE
      - CAPACITY
  order:
    type: integer
  dynamicRatesPriceDTO:
    type: array
    items:
      $ref: 'dynamic-rates-price.yml'
  translationsDTO:
    type: array
    items:
      $ref: 'dynamic-price-translation.yml'

type: object
properties:
    isActive:
      type: boolean
      description: Indicates whether the price zone is active
    idPriceZone:
      type: integer
      description: Unique identifier for the price zone
    activeZone:
      type: integer
      description: Active zone ID
    dynamicPricesDTO:
      type: array
      description: List of dynamic prices associated with the price zone
      items:
        $ref: 'dynamic-prices.yml'

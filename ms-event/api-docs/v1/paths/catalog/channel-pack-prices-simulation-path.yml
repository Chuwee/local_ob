get:
  summary: Get catalog pack price simulation
  description: Get catalog pack price simulation
  tags:
    - Catalog Search
  parameters:
    - in: path
      name: channelId
      description: Channel Id
      schema:
        type: integer
        format: int64
      required: true
    - in: path
      name: packId
      description: Pack Id
      schema:
        type: integer
        format: int64
      required: true
  responses:
    '200':
      description: Get channel catalog pack price simulation
      content:
        application/json:
          schema:
            $ref: ../../models/catalog/channel-pack-prices-simulation/channel-pack-prices-simulation.yml
    '400':
      description: Bad parameter
      content:
        application/json:
          schema:
            $ref: ../../models/common/error.yml
    '404':
      description: Channel not found
      content:
        application/json:
          schema:
            $ref: ../../models/common/error.yml
get:
  summary: Get catalog pack
  description: Get catalog pack
  tags:
    - Catalog Search
  parameters:
    - in: path
      name: channelId
      description: Channel Id
      schema:
        type: integer
        format: int64
      required: true
    - in: path
      name: packId
      description: Pack Id
      schema:
        type: integer
        format: int64
      required: true
  responses:
    '200':
      description: Channel catalog pack
      content:
        application/json:
          schema:
            $ref: ../../models/catalog/channel-pack/channel-pack.yml
    '400':
      description: Bad parameter
      content:
        application/json:
          schema:
            $ref: ../../models/common/error.yml
    '404':
      description: Channel not found
      content:
        application/json:
          schema:
            $ref: ../../models/common/error.yml
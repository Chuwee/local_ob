Chips Filter Directive
============

Directive that get the filters modifications to show it as chips with combination of Chips Component.

The UI component allows to remove the currently active filters one by one by clicking its delete icon or to remove all together by clicking the "Remove all filters" action button.

The component attaches to **ListFiltersService** modifications to get active filters. It shows only filters that has a *label*.


{
  "extends": ["../../../../../.eslintrc.json"],
  "ignorePatterns": ["!**/*", "./.storybook/**"],
  "overrides": [
    {
      "files": ["*.ts"],
      "rules": {
        "@angular-eslint/prefer-standalone": "off"
      }
    }
  ]
}

export interface MstrUrls {
    load: string;
    login: string;
    logout: string;
    service_preview?: string;
    can_impersonate?: boolean;
    mstr_user_has_subscriptions?: boolean;
    superset?: boolean;
}

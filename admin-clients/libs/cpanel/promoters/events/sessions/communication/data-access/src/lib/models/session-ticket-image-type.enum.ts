export enum SessionTicketPdfImageType {
    body = 'BODY',
    bannerMain = 'BANNER_MAIN',
    bannerSecondary = 'BANNER_SECONDARY'
}

export enum SessionTicketPrinterImageType {
    body = 'BODY',
    bannerMain = 'BANNER_MAIN'
}

export enum SessionTicketPassbookImageType {
    strip = 'STRIP'
}
export enum FormControlNames {
    capacity = 'capacity',
    name = 'name',
    sector = 'sector',
    quota = 'quota',
    capacityDelta = 'capacityDelta'
}

export enum FormGroupNames {
    nnz = 'nnz',
    quota = 'quota'
}
